apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: britlearn-site
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/your-org/britlearn-site.git
    targetRevision: main
    path: k8s
    kustomize:
      images:
        - updateinformatica/britlearn-site:latest
  destination:
    server: https://kubernetes.default.svc
    namespace: britlearn-academy-site
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
