services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    restart: unless-stopped
    user: "0:0"
    command: >
      sh -c "chown -R www-data:www-data /var/www/html/data &&
             chmod -R 775 /var/www/html/data &&
             apache2-foreground"
